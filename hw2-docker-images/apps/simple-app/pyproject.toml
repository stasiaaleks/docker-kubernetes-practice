[project]
name = "simple-app"
version = "0.1.0"
description = "Async TCP chat room server"
requires-python = ">=3.13"
dependencies = [
    "pydantic-settings>=2.9.1",
    "python-dotenv>=1.1.0",
    "rich>=14.0.0",
]
